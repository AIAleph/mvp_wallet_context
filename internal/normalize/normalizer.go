package normalize

// Package normalize converts heterogeneous RPC payloads (tx, logs, traces)
// into the canonical ClickHouse schema rows. This stub will be expanded with
// table-safe typing (UTC timestamps, big integers) and ABI/topic decoders.

